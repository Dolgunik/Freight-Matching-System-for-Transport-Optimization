{
  "name": "freight-matching-prototype",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "npm --prefix backend test && npm --prefix frontend run build",
    "test:backend": "npm --prefix backend test",
    "test:frontend": "npm --prefix frontend run build"
  }
}

